// services/StripeService.java

public class StripeService {
    public void charge(double amount, String recipient) {
        // Lógica para processar pagamento com Stripe
        System.out.println("Processando pagamento de $" + amount + " para " + recipient + " via Stripe.");
    }
}
